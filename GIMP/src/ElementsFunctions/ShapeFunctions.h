#include <stdio.h>
#include <stdlib.h>
#include "../ToolsLib/Utils.h"

#ifndef TypeDefinitions
#define TypeDefinitions
#endif

Matrix L2(Matrix);

Matrix dL2(Matrix);

Matrix Q4(Matrix);

Matrix dQ4(Matrix);
