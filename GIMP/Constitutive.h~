#include <stdio.h>
#include <stdlib.h>
#include "Utils.h"

Tensor LinearElastic(double PoissonRatio,double YoungModulus){

  double LameFirstParam =
    PoissonRatio*YoungModulus/((1-PoissonRatio*2)*(1+PoissonRatio));
  double LameSecondParam =
    YoungModulus/(2*(1+PoissonRatio));

  Tensor D;

  D.xx =;
  D.xy =;
  D.yy =;
  
  return D;
}
